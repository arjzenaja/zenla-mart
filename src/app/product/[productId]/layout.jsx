import React from 'react'

export const metadata = {
  title: "PRODUCTS DETSISL PAGE",
  description: "PRODUCTS",
};

const ProductPageLayout = ({ children }) => {
  return (
    <>
      {children}
    </>
  )
}

export default ProductPageLayout
